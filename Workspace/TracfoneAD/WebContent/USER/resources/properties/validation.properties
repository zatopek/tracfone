# Define validation messages that are addressed in the 'messageClass'
# attribute of the validators. 'messageClass.' is a constant prefix.
# For example, to display the declared example message ('an example message')
# for a validator, set its 'messageClass' attribute to 'example'. 

messageClass.example=an example message

